const Navbar = () => {
  return (
    <nav className="navbar">
      <h1>Book List</h1>
      <div className="links">
        <a href="/">Home</a>
        <a href="/addBook">Add New Book</a>
      </div>
    </nav>
  );
};

export default Navbar;
